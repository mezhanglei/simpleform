import { defineConfig } from 'dumi';

export default defineConfig({
  outputPath: '../docs/docs',
  base: '/simpleform/docs/',
  publicPath: '/simpleform/docs/',
  themeConfig: {
    name: 'SF',
  },
});
